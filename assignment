import re

list_a = []

with open('result.txt',encoding='utf-8')as f:
    data = f.readline()

    # print(data[0:15]) # 時間だけを抽出したい

    p = r'length(.*)'  # 「length」の後ろにある文字だけを抽出したい
    m = re.search(p, data)
    # print(m.group(1))
    int(m.group(1))

list_row = [data[0:15],m.group(1)] 
list_a.append(list_row)
print(list_row) 
