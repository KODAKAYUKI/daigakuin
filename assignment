import csv
import pandas as pdp

with open('result.csv') as f:
    reader = csv.DictReader(f)
    for row in reader:
        if 0 < float(row['Time']) < 0.1:
            print(int(row['Length']))
            result_sum = sum(int(row['Length']))
            print(result_sum)
